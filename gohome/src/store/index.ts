import Vue from "vue";
import Vuex from "vuex";
import { Lazyload } from "vant";

Vue.use(Lazyload);
Vue.use(Vuex);

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: {}
});
